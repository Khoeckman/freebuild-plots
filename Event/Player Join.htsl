// Init: homePrefix
// Input: id, homeAddr
function "Spawn"
function "Default inventory" 

// Fix vars incase they were left incorrectly
if (var homeAddr > 0) {
  var homePrefix = "&a&oPlot &b&o#"
} else {
  var homePrefix unset
}

globalvar houseIgn = %player.name%
globalvar houseGroup = %player.group.tag%
globalvar houseVersion = %player.version%
function "Join" true

title "&a&oCreative &2&oPlots" "&e&lWELCOME" 0 2 1
sound "Level Up" 0.7 1.0 invokers_location