// @global
// Event input: reqId, reqAddr, reqPoints

// Let the requestor know the function is not on cooldown
if (globalvar reqId == var id) {
  var cd = 1
}

// ChatSocket only
if (!hasGroup "Dev" true) {
  exit
}

chat "[CS] {type:\"Plot rating\", plotAddr:%var.global/reqAddr%, points:%var.global/reqPoints%}"